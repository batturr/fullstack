# HTML MCQ - Set 4 (Intermediate Level)

**1. Geolocation is accessed from JavaScript via?**

a) `A <geo> element`
b) `navigator.geolocation (script API, not an HTML tag)`
c) `<location src="...">`
d) `meta name="geo"`

**Answer: b) `navigator.geolocation (script API, not an HTML tag)`**

---

**2. Web Storage (`localStorage` / `sessionStorage`) is?**

a) `Declared only in HTML attributes`
b) `A browser scripting API; persistence differs by storage type`
c) `A CSS feature`
d) `An HTTP header only`

**Answer: b) `A browser scripting API; persistence differs by storage type`**

---

**3. HTML5 Drag and Drop uses attributes such as?**

a) `copyable="true"`
b) `draggable and event handlers in script`
c) `drag="css"`
d) `<dragsource>`

**Answer: b) `draggable and event handlers in script`**

---

**4. History API is primarily used from?**

a) `<history>`
b) `history.pushState / replaceState in JavaScript`
c) `<meta http-equiv="history">`
d) `<link rel="history">`

**Answer: b) `history.pushState / replaceState in JavaScript`**

---

**5. `<canvas>` provides?**

a) `Vector graphics only`
b) `A scriptable bitmap surface for 2D/WebGL contexts`
c) `Built-in video player only`
d) `Server-side rendering`

**Answer: b) `A scriptable bitmap surface for 2D/WebGL contexts`**

---

**6. Inline SVG in HTML allows?**

a) `No styling with CSS`
b) `DOM-accessible vector graphics with CSS/JS`
c) `Only raster images`
d) `Only in <head>`

**Answer: b) `DOM-accessible vector graphics with CSS/JS`**

---

**7. `<embed>` vs `<object>`?**

a) `Identical in all browsers`
b) `Both embed resources; object supports params and richer fallback`
c) `Only embed works in HTML5`
d) `Deprecated in HTML5`

**Answer: b) `Both embed resources; object supports params and richer fallback`**

---

**8. `<map>` and `<area>` are used for?**

a) `Site maps`
b) `Clickable image maps`
c) `Table headers`
d) `Geo charts`

**Answer: b) `Clickable image maps`**

---

**9. `<datalist>` provides?**

a) `Server-side validation only`
b) `Predefined suggestions for an input`
c) `Video subtitles`
d) `Cookie list`

**Answer: b) `Predefined suggestions for an input`**

---

**10. `<output>` represents?**

a) `Console log`
b) `The result of a calculation or user action`
c) `HTTP response body`
d) `Print preview`

**Answer: b) `The result of a calculation or user action`**

---

**11. `<dialog>` element is for?**

a) `Server dialogs`
b) `Modal or non-modal dialog boxes`
c) `DNS resolution`
d) `Email threads`

**Answer: b) `Modal or non-modal dialog boxes`**

---

**12. `<template>` contents are?**

a) `Rendered immediately in layout`
b) `Inert until cloned/activated by script`
c) `Always visible`
d) `Only for SVG`

**Answer: b) `Inert until cloned/activated by script`**

---

**13. Shadow DOM `<slot>` (with Web Components) allows?**

a) `Database slots`
b) `Projecting light DOM children into shadow tree placeholders`
c) `Cookie storage`
d) `iframe sizing`

**Answer: b) `Projecting light DOM children into shadow tree placeholders`**

---

**14. Custom Elements are part of?**

a) `HTML4`
b) `Web Components (custom element definitions in script)`
c) `RSS`
d) `WebSQL only`

**Answer: b) `Web Components (custom element definitions in script)`**

---

**15. Microdata `itemtype` often uses?**

a) `A CSS class name`
b) `A vocabulary URL (e.g. schema.org)`
c) `A JPEG URL`
d) `An IP address`

**Answer: b) `A vocabulary URL (e.g. schema.org)`**

---

**16. Open Graph tags commonly use which attribute on `<meta>`?**

a) `name`
b) `property`
c) `rel`
d) `href`

**Answer: b) `property`**

---

**17. Twitter Card tags often use?**

a) `meta tags with names like twitter:card and twitter:title`
b) `only a <twitter> element`
c) `http-equiv="twitter"`
d) `role="twitter"`

**Answer: a) `meta tags with names like twitter:card and twitter:title`**

---

**18. Responsive viewport control uses?**

a) `<meta name="viewport" content="width=device-width, initial-scale=1">`
b) `<viewport>`
c) `link rel="viewport"`
d) `script name="viewport"`

**Answer: a) `<meta name="viewport" content="width=device-width, initial-scale=1">`**

---

**19. Document character encoding should be declared early via?**

a) `comment <!-- utf-8 -->`
b) `meta charset early in the document (first 1024 bytes ideally)`
c) `title charset`
d) `body lang`

**Answer: b) `meta charset early in the document (first 1024 bytes ideally)`**

---

**20. `lang` on `<html>` helps?**

a) `Font licensing only`
b) `Screen readers, hyphenation, translation tools`
c) `TCP congestion`
d) `GPU picking`

**Answer: b) `Screen readers, hyphenation, translation tools`**

---

**21. Web App Manifest is linked with?**

a) `<link rel="manifest" href="manifest.webmanifest">`
b) `<meta manifest>`
c) `<script type="manifest">`
d) `<iframe manifest>`

**Answer: a) `<link rel="manifest" href="manifest.webmanifest">`**

---

**22. Service workers are registered from?**

a) `A dedicated HTML register element`
b) `JavaScript (navigator.serviceWorker.register); no dedicated registration tag`
c) `<meta service-worker>`
d) `<link rel="worker" href="sw.js">` only

**Answer: b) `JavaScript (navigator.serviceWorker.register); no dedicated registration tag`**

---

**23. `<link rel="preload" as="font" href="...">` hints?**

a) `Lazy load after onload`
b) `High-priority early fetch for critical resources`
c) `Delete the resource`
d) `Block crawlers`

**Answer: b) `High-priority early fetch for critical resources`**

---

**24. `<link rel="prefetch" href="...">` suggests?**

a) `Lower priority future navigation resource`
b) `Synchronous XML parse`
c) `Mandatory preload`
d) `CSP report`

**Answer: a) `Lower priority future navigation resource`**

---

**25. `<link rel="preconnect" href="https://example.com">` helps?**

a) `DNS + TLS early setup to origins`
b) `Inline images`
c) `Form validation`
d) `Shadow DOM`

**Answer: a) `DNS + TLS early setup to origins`**

---

**26. `loading="lazy"` on `<img>` / `<iframe>`?**

a) `Loads before parser`
b) `Defers loading until near the viewport`
c) `Disables the resource`
d) `Requires async on script`

**Answer: b) `Defers loading until near the viewport`**

---

**27. `async` on `<script src>` means?**

a) `Runs before parse, blocks HTML`
b) `Downloads in parallel; executes when ready (order not guaranteed)`
c) `Never executes`
d) `Only for modules`

**Answer: b) `Downloads in parallel; executes when ready (order not guaranteed)`**

---

**28. `defer` on classic scripts?**

a) `Same as async always`
b) `Download in parallel; execute after document is parsed, in order`
c) `Disables JavaScript`
d) `Only inside SVG`

**Answer: b) `Download in parallel; execute after document is parsed, in order`**

---

**29. `DOMContentLoaded` fires when?**

a) `All subresources (images) loaded`
b) `HTML parsed; DOM built (without waiting for all resources)`
c) `After service worker update`
d) `Never`

**Answer: b) `HTML parsed; DOM built (without waiting for all resources)`**

---

**30. `window` `load` event fires when?**

a) `Only HTML parsed`
b) `Full page and resources like images have loaded`
c) `Before any CSS`
d) `On click only`

**Answer: b) `Full page and resources like images have loaded`**

---

**31. What is the output structure?**

```html
<canvas id="c" width="2" height="2"></canvas>
```

a) `A 2×2 CSS pixel drawing surface (unless scaled)`
b) `Always full viewport`
c) `Invalid without <p>`
d) `Vector-only`

**Answer: a) `A 2×2 CSS pixel drawing surface (unless scaled)`**

---

**32. `<area shape="rect" coords="...">` belongs inside?**

a) `<table>`
b) `<map>` associated with an `<img usemap>`
c) `<form>`
d) `<svg>`

**Answer: b) `<map>` associated with an `<img usemap>`**

---

**33. `<datalist id="x">` links to input via?**

a) `src="x"`
b) `list="x"`
c) `for="x"`
d) `data-list="x"`

**Answer: b) `list="x"`**

---

**34. `<dialog open>` without script shows as?**

a) `Always hidden`
b) `Open by default (non-modal) in supporting browsers`
c) `Only in print`
d) `Invalid`

**Answer: b) `Open by default (non-modal) in supporting browsers`**

---

**35. JSON-LD structured data is usually embedded as?**

a) `<json>`
b) `<script type="application/ld+json">`
c) `<link rel="json">`
d) `<meta type="ld+json">`

**Answer: b) `<script type="application/ld+json">`**

---

**36. `http-equiv` on `<meta>` can simulate?**

a) `GPU hints`
b) `HTTP headers like refresh or content-type in some cases`
c) `WebSocket URL`
d) `ARIA roles`

**Answer: b) `HTTP headers like refresh or content-type in some cases`**

---

**37. Inline script without `async`/`defer` (classic) typically?**

a) `Runs after load event always`
b) `Blocks HTML parsing while fetched and executed (if external)`
c) `Never blocks`
d) `Only runs in workers`

**Answer: b) `Blocks HTML parsing while fetched and executed (if external)`**

---

**38. `type="module"` scripts are deferred by default?**

a) `No`
b) `Yes, module scripts defer like defer`
c) `Only in IE`
d) `They run in head before parse`

**Answer: b) `Yes, module scripts defer like defer`**

---

**39. `<object data="file.pdf">` can show fallback when?**

a) `Never`
b) `When the resource cannot be displayed; inner HTML is fallback`
c) `Only with canvas`
d) `Only on mobile`

**Answer: b) `When the resource cannot be displayed; inner HTML is fallback`**

---

**40. `<embed type="image/svg+xml" src="...">` is?**

a) `Invalid`
b) `An embedding element for external content`
c) `Same as <img> always`
d) `Table related`

**Answer: b) `An embedding element for external content`**

---

**41. Which link loads a stylesheet (render-blocking by default)?**

a) `<link rel="stylesheet" href="styles.css">`
b) `<style src="styles.css">`
c) `<css href="styles.css">`
d) `<script rel="stylesheet">`

**Answer: a) `<link rel="stylesheet" href="styles.css">`**

---

**42. `fetchpriority="high"` on an `<img>` suggests?**

a) `Lower download priority`
b) `Higher fetch priority relative to other images`
c) `Disable caching`
d) `Force lazy load`

**Answer: b) `Higher fetch priority relative to other images`**

---

**43. `integrity` on `<script>` / `<link>` enables?**

a) `Lazy loading`
b) `Subresource Integrity (hash verification)`
c) `Geolocation`
d) `WebRTC`

**Answer: b) `Subresource Integrity (hash verification)`**

---

**44. `crossorigin` on `<img>` / `<script>` affects?**

a) `Only print layout`
b) `CORS mode for cross-origin resources`
c) `Tab order`
d) `ARIA naming`

**Answer: b) `CORS mode for cross-origin resources`**

---

**45. `referrerpolicy` on elements like `<a>` or `<img>` controls?**

a) `Which Referrer header is sent`
b) `Cookie SameSite`
c) `TLS version`
d) `Video codec`

**Answer: a) `Which Referrer header is sent`**

---

**46. `nonce` on inline `<script>` / `<style>` is used with?**

a) `JSON-LD only`
b) `Content Security Policy allowing specific inline content`
c) `Service worker scope`
d) `Canvas alpha`

**Answer: b) `Content Security Policy allowing specific inline content`**

---

**47. For `<iframe>`, `sandbox` attribute?**

a) `Enables all privileges`
b) `Applies a restricted permission set unless tokens allow more`
c) `Only affects SEO`
d) `Replaces srcdoc`

**Answer: b) `Applies a restricted permission set unless tokens allow more`**

---

**48. `srcdoc` on `<iframe>`?**

a) `Sets inline HTML content for the frame`
b) `Loads a manifest`
c) `Defines CSP only`
d) `Invalid attribute`

**Answer: a) `Sets inline HTML content for the frame`**

---

**49. `postMessage` is used for?**

a) `Sending UDP packets`
b) `Cross-document messaging including iframes`
c) `SVG path data`
d) `WebGL shaders`

**Answer: b) `Cross-document messaging including iframes`**

---

**50. Parser-blocking scripts in `<head>` without defer/async can delay?**

a) `DNS only`
b) `First paint and DOMContentLoaded`
c) `HTTPS only`
d) `ARIA computation only`

**Answer: b) `First paint and DOMContentLoaded`**

---

</think>
Cleaning up messy entries in Set4.

<｜tool▁calls▁begin｜><｜tool▁call▁begin｜>
Read