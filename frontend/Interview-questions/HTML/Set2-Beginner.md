# HTML MCQ - Set 2 (Beginner Level)

**1. Which input type provides a date picker in supporting browsers?**

a) `type="calendar"`
b) `type="datetime"`
c) `type="date"`
d) `type="day"`

**Answer: c) `type="date"`**

---

**2. Which input lets the user pick a color?**

a) `type="rgb"`
b) `type="palette"`
c) `type="color"`
d) `type="paint"`

**Answer: c) `type="color"`**

---

**3. Which input shows a slider for numeric values in a range?**

a) `type="slider"`
b) `type="range"`
c) `type="scale"`
d) `type="scroll"`

**Answer: b) `type="range"`**

---

**4. Which input allows selecting a file from the device?**

a) `type="upload"`
b) `type="file"`
c) `type="binary"`
d) `type="path"`

**Answer: b) `type="file"`**

---

**5. Which input type holds a value not shown to the user?**

a) `type="secret"`
b) `type="hidden"`
c) `type="invisible"`
d) `type="none"`

**Answer: b) `type="hidden"`**

---

**6. What does `type="reset"` on an input typically do?**

a) `Submits the form`
b) `Clears/resets form controls to initial values`
c) `Deletes the server record`
d) `Refreshes the page`

**Answer: b) `Clears/resets form controls to initial values`**

---

**7. Which element creates a multi-line text input?**

a) `<input type="textarea">`
b) `<text>`
c) `<textarea>`
d) `<multiline>`

**Answer: c) `<textarea>`**

---

**8. Which structure defines a dropdown list?**

a) `<dropdown>` and `<item>`
b) `<select>` and `<option>`
c) `<list>` and `<choice>`
d) `<menu>` only

**Answer: b) `<select>` and `<option>`**

---

**9. What does the `for` attribute on `<label>` do?**

a) `Sets the form action`
b) `Associates the label with an element by matching its id`
c) `Defines tab order`
d) `Sets placeholder text`

**Answer: b) `Associates the label with an element by matching its id`**

---

**10. What wraps related form controls with a caption?**

a) `<group>` and `<caption>`
b) `<fieldset>` and `<legend>`
c) `<form>` and `<title>`
d) `<section>` and `<h2>` only

**Answer: b) `<fieldset>` and `<legend>`**

---

**11. What does the `placeholder` attribute show?**

a) `The submitted value name`
b) `Hint text inside the control when empty`
c) `Error message`
d) `Required marker`

**Answer: b) `Hint text inside the control when empty`**

---

**12. What does `required` on an input enforce?**

a) `Server must respond with 200`
b) `The field must have a value before submit (client-side)`
c) `The field is readonly`
d) `The field is hidden`

**Answer: b) `The field must have a value before submit (client-side)`**

---

**13. `method="post"` on a form typically sends data how?**

a) `In the URL query string only`
b) `In the request body`
c) `As cookies only`
d) `It cannot send data`

**Answer: b) `In the request body`**

---

**14. Can one `id` value be reused on multiple elements?**

a) `Yes, recommended`
b) `No, id should be unique in the document`
c) `Only inside tables`
d) `Only on inputs`

**Answer: b) `No, id should be unique in the document`**

---

**15. What is the primary purpose of `class`?**

a) `Unique identifier for one element`
b) `Grouping for styling/scripting; reusable across elements`
c) `Sets the HTTP method`
d) `Defines the tab index`

**Answer: b) `Grouping for styling/scripting; reusable across elements`**

---

**16. Which element embeds another HTML page?**

a) `<frame>`
b) `<iframe>`
c) `<embed>` only for HTML pages
d) `<page>`

**Answer: b) `<iframe>`**

---

**17. Which tag embeds audio content?**

a) `<sound>`
b) `<mp3>`
c) `<audio>`
d) `<media>`

**Answer: c) `<audio>`**

---

**18. Which tag embeds video content?**

a) `<movie>`
b) `<media type="video">`
c) `<video>`
d) `<clip>`

**Answer: c) `<video>`**

---

**19. Difference between `<br>` and `<hr>`?**

a) `Both create lists`
b) `<br>` breaks a line; `<hr>` is a thematic horizontal break`
c) `They are identical`
d) `<hr>` is only for tables`

**Answer: b) `<br>` breaks a line; `<hr>` is a thematic horizontal break`**

---

**20. Which element preserves whitespace and line breaks?**

a) `<code>`
b) `<pre>`
c) `<p>`
d) `<span>`

**Answer: b) `<pre>`**

---

**21. Which element marks a fragment of computer code (inline)?**

a) `<pre>` only
b) `<code>`
c) `<script>`
d) `<terminal>`

**Answer: b) `<code>`**

---

**22. What does `<abbr title="...">` provide?**

a) `A link target`
b) `An abbreviation with an expansion hint via title`
c) `Bold text`
d) `Image alt text`

**Answer: b) `An abbreviation with an expansion hint via title`**

---

**23. Which element indicates quoted content from another source?**

a) `<quote>`
b) `<q>` only for block
c) `<blockquote>`
d) `<cite>` for long quotes only

**Answer: c) `<blockquote>`**

---

**24. Which tags are used for superscript and subscript?**

a) `<sup>` and `<sub>`
b) `<up>` and `<down>`
c) `<super>` and `<subscript>`
d) `<small>` and `<big>`

**Answer: a) `<sup>` and `<sub>`**

---

**25. Which is a valid named color for legacy HTML color attributes?**

a) `neon`
b) `red`
c) `purpleish`
d) `rgb(255,0,0)` as a literal name

**Answer: b) `red`**

---

**26. Where do you declare a favicon link?**

a) `Only in <body>`
b) `Typically in <head> via <link rel="icon">`
c) `Inside <footer>`
d) `In robots.txt`

**Answer: b) `Typically in <head> via <link rel="icon">`**

---

**27. Which tag links an external stylesheet?**

a) `<style href="...">`
b) `<link rel="stylesheet" href="...">`
c) `<css src="...">`
d) `<import href="...">`

**Answer: b) `<link rel="stylesheet" href="...">`**

---

**28. Where should `<script src="app.js">` usually be placed for loading behavior?**

a) `Only before <!DOCTYPE>`
b) `Often before </body> or with defer in head`
c) `Inside <title>`
d) `Inside <meta>`

**Answer: b) `Often before </body> or with defer in head`**

---

**29. Primary purpose of `<!DOCTYPE html>`?**

a) `Loads jQuery`
b) `Triggers standards mode in browsers`
c) `Sets the page language`
d) `Defines JSON-LD`

**Answer: b) `Triggers standards mode in browsers`**

---

**30. Which nesting is invalid in HTML?**

a) `<div><p>Hi</p></div>`
b) `<p><div>Hi</div></p>`
c) `<ul><li>Hi</li></ul>`
d) `<span><a href="#">x</a></span>`

**Answer: b) `<p><div>Hi</div></p>`**

---

**31. Which elements are void (no end tag in HTML)?**

a) `<div>` and `<span>`
b) `<br>` and `<img>`
c) `<p>` and `<li>`
d) `<script>` and `<style>`

**Answer: b) `<br>` and `<img>`**

---

**32. XHTML compared to HTML5 serialization often required?**

a) `Lowercase only DOCTYPE`
b) `Self-closing slash on void elements and stricter parsing rules`
c) `No use of CSS`
d) `Mandatory <iframe>`

**Answer: b) `Self-closing slash on void elements and stricter parsing rules`**

---

**33. What does this output in the page?**

```html
<p>Hello<br>World</p>
```

a) `HelloWorld on one line`
b) `Hello then a line break then World`
c) `Error`
d) `A horizontal rule`

**Answer: b) `Hello then a line break then World`**

---

**34. Which `input` submits a form when clicked?**

a) `type="button"`
b) `type="submit"`
c) `type="click"`
d) `type="send"`

**Answer: b) `type="submit"`**

---

**35. `multiple` on `<select>` allows what?**

a) `Only one option`
b) `Selecting more than one option (with UI support)`
c) `Hidden options`
d) `Disables the select`

**Answer: b) `Selecting more than one option (with UI support)`**

---

**36. `selected` on `<option>` does what?**

a) `Hides the option`
b) `Pre-selects that option by default`
c) `Makes it readonly`
d) `Submits twice`

**Answer: b) `Pre-selects that option by default`**

---

**37. `value` on `<option>` is used for what?**

a) `CSS class name`
b) `The submitted value if different from visible text`
c) `Image URL`
d) `Tab order`

**Answer: b) `The submitted value if different from visible text`**

---

**38. Label association without `for`?**

a) `Impossible`
b) `Wrap the control inside the <label>`
c) `Use title only`
d) `Use placeholder only`

**Answer: b) `Wrap the control inside the <label>`**

---

**39. `enctype` on form is needed for file upload when using?**

a) `method="get"`
b) `multipart/form-data`
c) `application/json`
d) `text/plain` only

**Answer: b) `multipart/form-data`**

---

**40. `autocomplete="off"` on a form field suggests?**

a) `Field is required`
b) `Browser should not suggest prior values`
c) `Field is hidden`
d) `Field is readonly`

**Answer: b) `Browser should not suggest prior values`**

---

**41. `disabled` on an input means?**

a) `Value still submits`
b) `Control is not interactive and is not submitted`
c) `Same as readonly`
d) `Only works on buttons`

**Answer: b) `Control is not interactive and is not submitted`**

---

**42. `readonly` on text inputs means?**

a) `Not visible`
b) `User cannot edit but value can still submit`
c) `Same as disabled`
d) `Clears on submit`

**Answer: b) `User cannot edit but value can still submit`**

---

**43. `maxlength` limits what?**

a) `File size`
b) `Number of characters typed`
c) `Number of options`
d) `HTTP headers`

**Answer: b) `Number of characters typed`**

---

**44. `cols` and `rows` on `<textarea>` control?**

a) `Font size`
b) `Visible width and height (in characters/lines approximately)`
c) `Border color`
d) `HTTP method`

**Answer: b) `Visible width and height (in characters/lines approximately)`**

---

**45. `src` on `<iframe>` points to?**

a) `CSS file`
b) `Embedded document URL`
c) `Image only`
d) `Font file`

**Answer: b) `Embedded document URL`**

---

**46. `<source>` inside `<audio>` or `<video>` provides?**

a) `Table columns`
b) `Alternative media resources (e.g. formats)`
c) `Form validation`
d) `Meta keywords`

**Answer: b) `Alternative media resources (e.g. formats)`**

---

**47. `controls` attribute on `<video>` shows?**

a) `Subtitle track only`
b) `Browser playback UI`
c) `Print dialog`
d) `Nothing`

**Answer: b) `Browser playback UI`**

---

**48. `<q>` vs `<blockquote>`?**

a) `Both are block by default`
b) `<q>` is typically inline; `<blockquote>` is a block quote`
c) `Identical`
d) `<q>` is for tables`

**Answer: b) `<q>` is typically inline; `<blockquote>` is a block quote`**

---

**49. `type="module"` on `<script>` indicates?**

a) `Runs in IE6 mode`
b) `JavaScript module (import/export)`
c) `CSS module`
d) `WebAssembly only`

**Answer: b) `JavaScript module (import/export)`**

---

**50. `nomodule` on `<script>` is used to?**

a) `Disable CSS`
b) `Provide fallback for browsers that do not support modules`
c) `Block iframes`
d) `Force strict XML`

**Answer: b) `Provide fallback for browsers that do not support modules`**

---

</think>
Fixing a messy question in Set2 and completing the remaining sets.

<｜tool▁calls▁begin｜><｜tool▁call▁begin｜>
StrReplace