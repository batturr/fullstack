# Tailwind CSS v4 Multiple Choice Questions (MCQs)

A comprehensive collection of **300 Tailwind CSS v4 MCQs** organized into 6 sets of 50 questions each, across three difficulty levels.

> All questions target **Tailwind CSS v4** (2025) — featuring CSS-first configuration with `@theme`, `@import "tailwindcss"`, automatic content detection, the Oxide engine, OKLCH colors, and more.

## Beginner Level (100 Questions)

| Set | Topics | Link |
|-----|--------|------|
| Set 1 | Utility-first approach, v4 installation (`@tailwindcss/vite`, `@tailwindcss/postcss`, `@tailwindcss/cli`), `@import "tailwindcss"`, spacing, sizing, typography, colors, borders, display, flexbox, responsive prefixes | [Set 1 - Beginner](./Set1-Beginner.md) |
| Set 2 | State variants (hover/focus/active), grid, positioning, shadows, rings, transitions, transforms, object-fit, aspect-ratio, group/peer, divide, `not-*` variant, `@container` intro | [Set 2 - Beginner](./Set2-Beginner.md) |

## Intermediate Level (100 Questions)

| Set | Topics | Link |
|-----|--------|------|
| Set 3 | `@theme` directive, CSS custom properties (`--color-*`, `--spacing-*`), `@utility`, `@variant`, `@plugin`, `@source`, arbitrary values, dark mode in v4, container queries (`@min-*`/`@max-*`), `not-*`/`in-*` variants, gradients, cascade layers, OKLCH | [Set 3 - Intermediate](./Set3-Intermediate.md) |
| Set 4 | Animations, custom `@keyframes`, gradients (conic/radial), `has-*`/`group-has-*`, `data-*`/`supports-*` variants, accessibility variants (forced-colors, motion-reduce, contrast-more), 3D transforms, `field-sizing-content`, composable variants, print/portrait/landscape | [Set 4 - Intermediate](./Set4-Intermediate.md) |

## Expert Level (100 Questions)

| Set | Topics | Link |
|-----|--------|------|
| Set 5 | Oxide engine (Rust/Lightning CSS), auto content detection, `@source`/`@source not`, `@theme inline`/`reference`, `@utility`/`@variant` deep dive, `@plugin` vs `@config`, cascade layers architecture, `@apply` scoping, design systems, performance (Vite vs PostCSS), framework integration, `@starting-style`, `@property` | [Set 5 - Expert](./Set5-Expert.md) |
| Set 6 | v3→v4 migration (`@tailwindcss/upgrade`), breaking changes, PostCSS pipeline changes, Vite vs PostCSS tradeoffs, monorepo setups, advanced dark mode, a11y variants, named container queries, OKLCH theming, `prefix(tw)`, `source(...)`, dynamic class anti-patterns, `@apply` edge cases, specificity | [Set 6 - Expert](./Set6-Expert.md) |
