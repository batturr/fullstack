# TypeScript 5.9 Multiple Choice Questions (MCQs)

A comprehensive collection of **300 TypeScript MCQs** organized into 6 sets of 50 questions each, across three difficulty levels.

> All questions target **TypeScript 5.9** (December 2025) — covering `import defer`, `--module node20`, updated `tsc --init`, and features through the latest release. Also includes awareness of TypeScript 6.0/7.0 direction.

## Beginner Level (100 Questions)

| Set | Topics | Link |
|-----|--------|------|
| Set 1 | TypeScript basics, installation, `tsc --init` (5.9), basic types, type annotations, inference, arrays, tuples, enums, unions, literal types, aliases, interfaces, optional/readonly, functions, type assertions, tsconfig basics | [Set 1 - Beginner](./Set1-Beginner.md) |
| Set 2 | Classes, inheritance, access modifiers, abstract classes, generics basics, `keyof`, index signatures, overloads, intersections, discriminated unions, type narrowing, type guards, `satisfies`, `import type`, `as const`, template literal types, utility types (Partial, Required, Pick, Omit, Record) | [Set 2 - Beginner](./Set2-Beginner.md) |

## Intermediate Level (100 Questions)

| Set | Topics | Link |
|-----|--------|------|
| Set 3 | Advanced generics, conditional types, `infer`, mapped types, template literal types, utility types (ReturnType, Parameters, Awaited), `satisfies` in depth, `as const` in depth, discriminated unions exhaustive checking, function overloads, declaration merging, module augmentation, strict mode options, `unknown` vs `any`, advanced narrowing | [Set 3 - Intermediate](./Set3-Intermediate.md) |
| Set 4 | Decorators (stage 3), `using` keyword (Explicit Resource Management), `const` type parameters, `NoInfer<T>`, variance annotations (`in`/`out`), assertion functions, `verbatimModuleSyntax`, `--module node20` (5.9), `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, branded types, distributive conditional types, recursive types, `never` type | [Set 4 - Intermediate](./Set4-Intermediate.md) |

## Expert Level (100 Questions)

| Set | Topics | Link |
|-----|--------|------|
| Set 5 | `import defer` (5.9), `--module node20` (5.9), lib.d.ts ArrayBuffer changes (5.9), TS 5.8 return branch checks, type-level programming, HKT patterns, advanced mapped types (key remapping), variadic tuples, covariance/contravariance, `infer extends`, recursive types, module resolution (bundler vs nodenext vs node20), type instantiation depth, performance | [Set 5 - Expert](./Set5-Expert.md) |
| Set 6 | TypeScript 7.0 native port (Go), TS 6.0 transition, advanced decorators, `using`/`await using` patterns, advanced `satisfies`, control flow analysis edge cases, `NoInfer<T>` advanced, deep path types, Result pattern, declaration files, project references, `composite`/`incremental`, iterator/generator types, Proxy/Reflect typing, excess property checking | [Set 6 - Expert](./Set6-Expert.md) |
