# 🔵 **What is a Tag in HTML?**

* A **tag** is a special keyword used in HTML.
* Tags are written inside **angle brackets**: `< >`
* They are **instructions or commands** given to the web browser.
* Tags tell the browser **how to display content** on the webpage (text, images, links, headings, lines, tables, etc.).
* Every element you see on a webpage is created using some tag.

### ✅ **Syntax**

```
<tagname>
```

---

# 🔵 **Types of HTML Tags**

HTML tags are mainly of **two types**:

---

## **1️⃣ Paired Tags (Container Tags)**

These tags have **two parts**:

* **Opening tag** → `<tag>`
* **Closing tag** → `</tag>`

### ✔️ Key Points

* They **wrap content** between the opening and closing tag.
* Closing tag always contains a **forward slash ( / )**.
* Used when you want to **format, group, or display text**.
* The content between the pair is what appears on the screen.

### ✔️ Examples

```
<h1>Hello</h1>
<p>This is a paragraph.</p>
<b>Bold Text</b>
<i>Italic Text</i>
<div>Container</div>
```

---

## **2️⃣ Unpaired Tags (Empty Tags / Self-closing Tags)**

* These tags do **not** have a closing tag.
* They are **standalone**.
* Used for inserting elements that **do not require text inside**.
* They perform an **instant action** (like breaking a line, inserting a line, image, etc.).

### ✔️ Examples

```
<br>  — line break
<hr>  — horizontal line
<img> — image tag
<meta> — metadata tag
<input> — form input field
```

---

# 🔵 **Why Tags Are Important**

* They form the **structure of every HTML page**.
* They control **content formatting** (bold, italic, underline, headings).
* They help add **media** (images, audio, video).
* They help create **forms** (input, button, select).
* They define **layout** (div, section, article).
* They help browsers **understand page structure**.

---

# 🔵 **Simple Example Explained**

```
<h1>Welcome to HTML</h1>
```

### Breakdown:

* `<h1>` → Opening tag
* `Welcome to HTML` → Content
* `</h1>` → Closing tag
